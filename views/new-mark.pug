doctype html
html
  head
    title= title
    include includes/head.pug
    script(type='text/javascript', src='/javascripts/mark.js')
  body

    include includes/top-menu.pug

    .pure-form.pure-form-stacked
      fieldset
        legend 新建任务
        label 任务标题
        input(type='text' name='title')
        .bottom-space
        label 被考核人
        ul(id='players')
        button.pure-button(onclick='app.selectPlayers();') 选择被考核人
        .bottom-space

        label 考核项目
          i.fa.fa-plus-circle.add-item(onclick='app.displayAllItems();')

      hr
      .bottom-space
      button.pure-button.right-space(onclick='#') 确定提交
      a.pure-button(href='/marks') 返回

    .end

    .all-items.hidden
      span.top(onclick='app.hideAllItems();') 关闭
      .loading
        span 正在加载已有项目列表, 请稍等.
      ul
      span.bottom(onclick='app.hideAllItems();') 关闭
